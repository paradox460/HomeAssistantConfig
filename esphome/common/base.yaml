# Base configuration package for all ESPHome devices
# Includes: ESPHome setup, logger, API, and OTA updates

# Basic ESPHome setup

esphome:
  name: "${name}"
  friendly_name: "${friendly_name}"

# Enable logging
logger:

# Enable Home Assistant API
api:

# Allow Over-The-Air updates
ota:
  - platform: esphome
